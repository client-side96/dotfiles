new_tab Mobile App

cd ~/devel/care/app-mobile

launch --title "App (Editor)" --hold zsh
launch --title "App (CLI)" --hold zsh


new_tab Middleware

cd ~/devel/care/app-middleware

launch --title "Middleware (Editor)" --hold zsh
launch --title "Middleware (CLI)" --hold zsh


new_tab Chat API

cd ~/devel/care/virtual-coach-app

launch --title "Chat API (Editor)" --hold zsh
launch --title "Chat API (CLI)" --hold zsh


new_tab Terraform

cd ~/devel/care/app-middleware-deployment

launch --title "Terraform (Editor)" --hold zsh
launch --title "Terraform (CLI)" --hold zsh

